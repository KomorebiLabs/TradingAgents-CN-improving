# 屎山报告 4/3（加更）：Phase 0–3 重构施工记录

> 施工日期：2026-08-16
> 依据：`屎山报告-3-清理与重构路线.md` 的 Phase 0 / 1 / 2 / 3（部分）
> 原则：只改代码 + 新增文件，**不删任何文件**（删除由你执行）
> 验证：188 个测试全部通过；CLI 实测；grep 全面审计；gitnexus detect-changes 核对影响面

---

## 一、本次完成的工作

### Phase 0：入口可预测 ✅

| 步骤 | 内容 | 文件 |
|---|---|---|
| 0.1 | 删除全部 3 处 `try/except ImportError` 静默降级（新代码的 bug 从此会直接炸出来，不再偷偷换旧引擎） | `tradingagents/__main__.py`、`cli/main_menu.py` |
| 0.2 | 安装命令改指新统一 CLI：`tradingagents = "tradingagents.__main__:app"`；screener 用 `app.add_typer()` 正式注册为子应用，**彻底删除 `sys.argv` 篡改**，`python -m tradingagents screener run --date X` 原生可用 | `pyproject.toml`、`tradingagents/__main__.py`、`cli/screener/app.py`（新增 `screener_app` + `run` 子命令） |
| 0.3 | 版本单一来源：删掉手写 `2.0.0`，`__version__` 改读包元数据（现为 0.2.3），并加防回归测试 | `tradingagents/__init__.py` |
| 0.4 | 清除全部生产调试打印：`cli/analyze/run_impl.py`（6 处）、`cli/analyze/app.py`（2 处）、`tradingagents/ui/live_dashboard.py`（约 24 处）、`tradingagents/screener/universe.py`（5 处）、`cli/screener/run_impl.py`（1 处裸 print 转为 console 样式） | 见左 |
| 0.4+ | 删除 `dataflows/interface.py` 死代码：`_call_screener_data_access` / `_screener_callable`（无调用点）+ `route_to_vendor` 内恒假的 alias 换回块 | `tradingagents/dataflows/interface.py` |
| 附加 | `analyze --ticker X --no-interactive` 现在真正工作（默认配置直跑，不再是无声忽略的死参数） | `tradingagents/__main__.py` |

### Phase 1：新旧 CLI 收口 ✅

1. **断奶**：`cli/analyze/app.py` 的 announcements 从旧层改回本层 `cli.announcements`（该文件本就是完整实现，激活即可）；
2. **报表查看器迁移**：`commands/report/html_builder.py` 的实现移入新文件 `cli/report_viewer.py`，`__main__.py` 与 `main_menu.py` 改为从新层导入；
3. **`cli/main.py` 重写**：从"import 时 sys.exit(1) + 转发旧引擎"改为 3 行薄转发到新 Analyzer；
4. **`cli/__init__.py` 清空旧层重定向**（原先 lazy-import 旧 commands + sys.exit 副作用）。

**收口结果（grep 认证）**：活跃代码对 `tradingagents/commands/` 与 `tradingagents/screener/cli/` 的真实 import = **0**。四对分叉文件（utils/stats_handler/models/config）不需要合并了——它们随旧层一起退役。

### Phase 2：统一图执行驱动 ✅

1. `TradingAgentsGraph` 新增公开流式 API `stream_analysis()`（内部类 `_AnalysisStream`）：迭代产出状态快照，耗尽后 `final_state` / `decision` / `result` 就绪；图执行失败时按 `propagate()` 语义落入 fallback 状态而不是中途炸掉仪表盘；
2. `propagate()` 重写为消费 `stream_analysis` 的 4 行薄封装——**官方驱动与新 CLI 驱动从此是同一条执行路径**（此前是两套平行实现）；
3. `cli/analyze/run_impl.py` 不再触碰图内部：删除对 `graph.graph`、`graph.propagator`、私有方法 `_synchronize_structured_state` 的所有直接调用，改用 `stream_analysis`。
4. 行为差异说明：新路径下仪表盘运行中图出错会显示错误状态（旧路径直接抛异常）；`process_signal` 失败回落为 `decision="N/A"`（旧路径静默吞掉后同样显示 N/A）。

### Phase 3（部分）：契约收口 ✅

1. **配置深合并 + 真 bug 修复**：`TradingAgentsGraph.__init__` 改为 `deepcopy(DEFAULT_CONFIG)` 再合并传入配置——①传部分配置不再 KeyError；②实例配置写入不再污染全局 `DEFAULT_CONFIG`（旧代码 `config=None` 时直接引用全局默认值，多个图实例互相污染）；
2. **`dataflows/config.py` 从 368 行瘦身到 55 行**（行为完全一致，作文注释清除，语义写入 docstring）；
3. **魔法阈值命名化**：`state_helpers.py` 的 6 个字符串长度阈值（4000/2500/3200/2200/3500/1600）提为具名常量（`RESEARCH_MANAGER_HISTORY_CHARS` 等），函数签名未变。

### 测试护栏（新建 `tests/`，188 个用例，6 秒跑完）✅

| 文件 | 覆盖 |
|---|---|
| `test_smoke_imports.py` | 15 个关键模块导入零输出、零 sys.exit；统一 CLI 命令注册完整性 |
| `test_version.py` | `__version__` == pyproject；禁止手写版本常量回流 |
| `test_state_helpers.py` | 压缩交接阈值路由冻结（characterization）；双写 helper 形状冻结 |
| `test_interface_routing.py` | 供应商类别/别名/优先级/未知方法拒绝；断言 dataflows 无 screener 反向引用 |
| `test_graph_stream.py` | `_AnalysisStream` 全路径：正常流、回调透传、历史注入、单次消费、异常→fallback、决策提取失败→N/A（全 stub，无 LLM 无网络） |
| `test_compile_all.py` | 全部活跃 .py 编译通过（旧层文件已排除，待删） |

---

## 二、gitnexus 影响面核对（AGENTS.md 要求）

`detect-changes`：16 文件 / 36 符号 / 8 条执行流，风险 **high** ——受影响流程全部是 CLI 入口链（`Main → _run_analyzer → run_analysis` 等），正是本次重构的目标范围，无越界。施工前对 `propagate`（LOW，实际 2 个调用方：`main.py`、`deep_analyzer.py:112`，均已保持兼容）、`route_to_vendor`（CRITICAL，31 个下游——本次仅删死代码，行为不变）、`_synchronize_structured_state`（HIGH，3 个调用方——其中 2 个正是本次收口对象）做过影响分析。

---

## 三、现在可以删的文件（你来操作）

删除前先跑一遍验收：`pytest -q`（188 绿）→ `python -m tradingagents --version` → `python -m tradingagents screener run --help` → `python -m tradingagents analyze --help`。

**旧层整树（已零引用认证）：**
- `tradingagents/commands/`（含 analyze、report 全部 12 个文件）
- `tradingagents/screener/cli/`（8 个文件）

**此前认证过的孤儿/垃圾（报告 3 清单不变）：**
- `tradingagents/agents/risk_mgmt/aggressive_debator.py`（拼写错误版孤儿）
- `requirements.txt`、`Dockerfile`、`docker-compose.yml`、`.env.enterprise.example`、`assets/wechat.png`、`assets/TauricResearch.png`、`uv.lock`
- 磁盘上的 `*-no/` 目录

**⚠️ 从可删清单中移除**：`cli/announcements.py` —— 它已被 `cli/analyze/app.py` 引用，是活代码。

---

## 四、遗留问题（下一轮施工目标）

> **2026-08-16 更新：第 1 条已在本轮完成，见下方"加更：Phase 3 后半"。**

1. ~~**状态双写仍是双写**：canonical 方向声明、`_synchronize_structured_state` 的退役计划（Phase 3 完整版）~~；
2. **数据层反向依赖**：`cn_indicators.py:25` 仍直接 import `ScreenerDataAccess` 且每次调用新建实例（限流/缓存失效）——等 `MarketDataPort`（Phase 4）；
3. **四模块依赖环**（interface → rag → cn_news_retriever → tools → interface）未解；
4. **171 处 `except Exception`** 未建立类型化错误边界；
5. `deep_analyzer.py` 与 `main.py` 仍走 `propagate()`（已统一到 stream 内核，无分叉风险，但值得知道）。

---

# 加更：Phase 3 后半——状态双写 canonical 化（2026-08-16 第二轮施工）

> 验证：**200 个测试全绿**（原 187 + 新增 13 个 canonical 契约测试，6.5 秒）
> gitnexus detect-changes：5 文件 / 37 符号 / 2 条流程，风险 **medium**（全部落在状态契约与 UI 读取，无越界）

## 做了什么

### 1. Canonical 政策正式立法（`agent_states.py`）

- 模块顶部新增 **Canonical State Policy** 声明块：`analyst_reports` / `debate_blocks` / `decision_blocks` / `ticker_info` 是唯一权威（canonical）；平铺字段（`market_report`、`investment_debate_state`、`final_trade_decision`……）降级为 legacy 读兼容镜像。
- 新增 `STATE_SCHEMA_VERSION = 2` 与 `AgentState.schema_version` 字段；`create_initial_state` 注入。
- 写明退役时间表：**v2（现在）全量双写 → v2.1 节点停止写平铺 → v3.0 删除平铺字段**。
- 新代码守则：写结构化（迁移期用 sync_* helper）、读结构化、冲突时结构化胜。

### 2. 修掉"平铺覆盖结构化"的 wipe 隐患（`trading_graph.py`）

`_synchronize_structured_state` 改名 **`_ensure_structured_state`**，方向语义重写：

- 旧逻辑：`analyst_reports["market"] = state.get("market_report", ...)` —— 平铺无条件覆盖结构化。未来任何"只写结构化"的节点会被空平铺值**抹掉**（这就是 canonical 化之前必须先修的地雷）；
- 新逻辑：**双向只补缺失，冲突时结构化胜**。结构化有值 → 回填平铺（保住 `setup.py` 路由、`reflection.py`、`signal_processing` 等 legacy 读方）；平铺有值而结构化缺失 → 填充结构化（保住 `_create_fallback_state` 这类 legacy 写方）；
- 辩论状态两形状保持**同一对象引用**（flat 读方自动跟随更新）；
- `schema_version` 缺失时补 2。

### 3. 日志瘦身（`_log_state`）

侦察确认 `full_states_log_*.json` **零代码读方**（纯人类阅读）后：

- 删除四份平铺报告、两份完整辩论状态、`trader_investment_decision`、`investment_plan` 的重复记录——同一份内容不再记两遍；
- 保留 `final_trade_decision` 单键作为 grep 便利入口；
- 新增 `schema_version` 与 `debate_blocks` 到日志；
- 顺带消灭了旧代码 `final_state["market_report"]` 式硬取键（缺键即崩）——全部改为 `.get()`。

### 4. UI 读取迁移（`cli/analyze/run_impl.py`）

`_update_analyst_statuses` 与 `_handle_debate_states` 改为**结构化优先、平铺兜底**读取（报告文件名/section key 保持平铺命名不变，输出文件零变化）。这也是"UI 消费 canonical 契约而非裸状态"路线的第一步。

### 5. 契约测试（`tests/test_state_canonical.py`，13 个用例）

冻结的契约：
- 结构化-only 写入被保留且镜像到平铺（wipe bug 的永久回归测试）；
- 平铺-only 写入（fallback state）填充结构化；
- 冲突时结构化胜；
- 辩论两形状同对象；
- 日志新形状（含"平铺重复键必须不存在"的断言）；
- UI 读取的结构化优先/平铺兜底两条路径；
- `schema_version` 的注入与回填。

## 对二开的直接意义

**从现在起，加一个新状态字段只需要：在 `agent_states.py` 的结构化块里声明 + 在节点里写入结构化 + 在 `_ensure_structured_state` 加一行映射（可选）。** 平铺镜像、legacy 读方、日志全部自动跟上——上一轮之前这是三处手工同步、漏一处就静默出错。

## 本轮文件清单

| 文件 | 变更 |
|---|---|
| `tradingagents/agents/utils/agent_states.py` | Canonical 政策 + schema_version |
| `tradingagents/graph/propagation.py` | 初始状态注入 schema_version |
| `tradingagents/graph/trading_graph.py` | `_ensure_structured_state` 重写 + `_log_state` 瘦身 |
| `cli/analyze/run_impl.py` | 两个 UI 读取函数迁移到结构化优先 |
| `tests/test_state_canonical.py` | 新增 13 个契约测试 |
| `tests/test_graph_stream.py` | stub 更名适配 |

## 建议提交

```bash
git add tradingagents/agents/utils/agent_states.py tradingagents/graph/propagation.py \
        tradingagents/graph/trading_graph.py cli/analyze/run_impl.py tests/
git commit -m "refactor(state): declare structured blocks canonical (schema v2)" -m "- agent_states: Canonical State Policy + STATE_SCHEMA_VERSION + retirement plan
- _synchronize_structured_state -> _ensure_structured_state:
  bidirectional fill-missing, structured wins (fixes flat-wipe hazard)
- _log_state: log structured once, drop duplicated flat keys (zero code readers)
- run_impl UI readers: structured-first with flat fallback
- tests: +13 canonical contract cases (200 total)"
```

---

# 加更二：Phase 4 首批——数据层端口与依赖环（2026-08-16 第三轮施工）

> 验证：**217 个测试全绿**（200 + 17 新增，8.5 秒）；`python -m tradingagents --version` 正常
> gitnexus detect-changes：3 文件 / 23 符号 / 6 条流程，风险 high——全部落在 `route_to_vendor` 的供应商路由链上，本次对其改动仅为错误类型（`VendorUnavailable` 是 `RuntimeError` 子类，现有捕获完全兼容）与周边死代码删除，行为保持

## 做了什么

### 1. `MarketDataPort` 端口落地（`tradingagents/ports/market_data.py`，新包）

- 按能力定义 Protocol（`fetch_hist` / `fetch_spot_snapshot` / `fetch_index_constituents` / `fetch_concept_boards` / `fetch_concept_constituents` / `fetch_industry_boards` / `fetch_fund_flow`），方法名与 `ScreenerDataAccess` 对齐，零实现导入；
- `get_market_data_port()` 返回**进程级共享实例**，`set_/reset_` 供测试注入；
- 默认工厂在**函数体内**延迟 import `ScreenerDataAccess`——模块级依赖图上 ports 不指向 screener（有回归测试钉死），运行期组合点集中在一处，未来供应商层重写只动工厂。

### 2. 修反向依赖 + 限流/缓存失效 bug（`cn_indicators.py`）

- `from tradingagents.screener.data_access import ScreenerDataAccess` 顶层导入删除，改走端口；
- 原实现**每次取行情新建 `ScreenerDataAccess`**——限流器状态和历史缓存随实例报废。现在共享实例让 `ThrottledRequester` 和 `_hist_cache` 真正生效；
- 有测试用 stub 端口验证 `get_cn_indicators` 全程走端口（离线），AST 扫描确保该文件永不回潮 screener 导入。

### 3. 四模块依赖环：连根拔（`interface.py`）

侦察发现比预想更彻底：`route_to_vendor_with_rag` 及其 6 个辅助函数（~80 行）**零调用方**——是被废弃的平行 RAG 尝试（活集成在 `rag_news_tools.py` / `rag_middleware.py`，调用的是普通 `route_to_vendor`）。整簇删除后：

```text
旧环：interface → rag → cn_news_retriever → cn_sector_news_tools → interface
现况：rag → tools → interface（单向，无环）
```

新的依赖图回归测试用 AST 扫描全部顶层 import + Kahn 拓扑，断言**全包无环**——这个环永远别想回来。

### 4. 类型化供应商错误（`dataflows/errors.py`）

- `VendorError(RuntimeError)` 基类 + `VendorUnavailable` / `VendorRateLimited` / `DataNotFound` / `VendorSchemaChanged`；
- 全部继承 `RuntimeError`——现有 `except RuntimeError` 捕获零破坏；
- `interface.py` 的两处 `RuntimeError` 改为 `VendorUnavailable`；`_is_rate_limit_error` 增加 `isinstance(VendorRateLimited)` 识别（保留旧的类名匹配兼容）；
- 这是"吞异常文化"治理的第一块砖：预期失败从此有名字，编程错误继续冒泡。

### 5. 新测试（+17）

| 文件 | 覆盖 |
|---|---|
| `test_market_data_port.py`（9） | 共享实例身份 / 注入与重置 / 协议符合性 / 默认适配器携带限流+缓存状态 / cn_indicators 无 screener 导入（AST）/ stub 端口端到端 |
| `test_dependency_graph.py`（4） | **全包模块级 import 图无环**（Kahn）/ dataflows 禁止 import screener / interface 禁止 import agents / ports 模块级禁止 import screener |
| `test_interface_routing.py`（+3） | 类型化错误抛出 / 继承兼容性 / 限流识别 |

施工中插曲：依赖图测试首版的相对导入解析和 Kahn 种子集有 bug，报出一个 65 模块的"假环"——用独立 DFS 诊断脚本交叉验证后修掉测试自身的两个缺陷，最终确认**全图真实无环**。这个测试本身也成了这次施工的受益者。

## 本轮文件清单

| 文件 | 变更 |
|---|---|
| `tradingagents/ports/__init__.py`、`ports/market_data.py` | 新增：端口 + 注册表 + 共享工厂 |
| `tradingagents/dataflows/errors.py` | 新增：类型化供应商错误 |
| `tradingagents/dataflows/cn_indicators.py` | 走端口，删 screener 依赖 |
| `tradingagents/dataflows/interface.py` | 删死 RAG 钩子（~80 行）+ 类型化错误 |
| `tests/`（3 个文件） | +17 测试 |

## 报告 2 七大根基的终局状态

| 根基 | 状态 |
|---|---|
| 1 状态双写 | ✅ canonical 化（v2 政策落地） |
| 2 三驱动器 | ✅ 已收口（Phase 2） |
| 3 ImportError 静默降级 | ✅ 已铲除（Phase 0） |
| 4 配置双中心 | 🟡 深合并已修，全局单例仍待收口（低优先） |
| 5 数据层反向依赖 + 环 | ✅ **环已断、反向依赖已走端口**；`ScreenerDataAccess` 本体拆分留给后续 |
| 6 双 CLI 分叉 | ✅ 旧层已物理删除 |
| 7 吞异常 + 作文注释 | 🟡 类型化错误第一块砖已铺；171 处宽泛捕获的逐类治理是长期活 |

## 建议提交

```bash
git add tradingagents/ports tradingagents/dataflows tests/
git commit -m "refactor(data): MarketDataPort seam, break import cycle, typed vendor errors" -m "- ports/market_data: capability Protocol + process-wide shared instance
  (fixes per-call ScreenerDataAccess rebuild disabling throttle/cache)
- cn_indicators: consume the port, drop dataflows->screener inversion
- interface: delete dead RAG hook (~80 lines, zero callers) — breaks the
  interface->rag->cn_news_retriever->tools->interface import cycle
- dataflows/errors: VendorError hierarchy (RuntimeError-compatible),
  interface raises VendorUnavailable; rate-limit detection typed
- tests: +17 (port contract, AST dependency-graph acyclicity guard, typed errors)"
```

## 五、建议的提交切分

```bash
# 1. 你的删除（先行提交，与重构分开）
git add -A && git commit -m "chore: remove retired docs/tests/reference dirs (manual cleanup)"

# 2. 本次重构（我未提交，留给你审后执行）
git add cli/ tests/ tradingagents/ pyproject.toml
git commit -m "refactor(cli): unify entrypoints, single version source, public streaming API

- remove try/except ImportError fallbacks (3 sites) and sys.argv munging
- console script -> tradingagents.__main__:app; screener registered via add_typer
- --ticker/--date/--no-interactive now functional for analyze
- move report viewer to cli/report_viewer.py; activate cli/announcements.py
- TradingAgentsGraph.stream_analysis() public API; propagate() reuses it;
  run_impl no longer touches graph internals
- config: deep-merge over defaults (fixes DEFAULT_CONFIG pollution + partial-config KeyError)
- state_helpers: named compression thresholds; dataflows/config.py 368->55 lines
- strip 38 debug prints from production paths; drop dead vendor-routing code
- tests/: 188 offline smoke/characterization cases"
```

---

# 加更三：Phase 4 主体——ScreenerDataAccess 1905 行拆分（2026-08-16 第四轮施工）

> 验证：**270 个测试全绿**（227 + 43 个新拆分层测试，23 秒）
> gitnexus：data_access 35 符号变更，影响 6 条流程（screener 引擎/策略消费链），风险 high——全部为行为保持型的结构搬移
> 公开 API 契约：**19 个公开方法名/签名/返回/fallback 顺序逐字保持**，外部零改动

## 拆分结果

| 文件 | 行数 | 变化原因（唯一） |
|---|---:|---|
| `data_access.py`（门面） | **546**（原 1905） | 公开 API + fallback 编排 + 缓存 |
| `capability.py` | 462 | 探测策略 / 能力矩阵 / 探测缓存 |
| `vendors/tencent.py` | 135 | 腾讯接口变更（AkShare 通道 + 直连通道） |
| `vendors/sina.py` | 150 | 新浪接口变更（含龙虎榜） |
| `vendors/ths.py` | 133 | 同花顺接口变更（概念码缓存随迁为模块级） |
| `vendors/misc.py` | 92 | 东财资金流 + 百度辅助数据 |
| `vendors/backup.py` | 73 | Baostock / yfinance 末位备源 |
| `response_parsers.py` | 252 | **纯函数**：THS 表格、腾讯 kline/行情/指数文本解析 |
| `ticker_formats.py` | 168 | **纯函数**：四种股票代码格式转换 + 日期标准化 |
| `vendor_http.py` | 85 | 礼貌策略（按源 sleep+抖动）、头伪装、腾讯裸 HTTP |

没有任何文件超过 550 行；每个文件只对一种变化敏感——路线图"ScreenerDataAccess 300-500 行、单一变化原因"的验收标准达成。

## 结构说明

- **门面只做编排**：`fetch_hist` 等公开方法 = 查缓存 → 按优先级试 vendors 函数 → 写缓存；`validate_interface_assumptions` 委托 capability 层；
- **探测与供应商解耦**：`capability.run_live_probes()` 只接受 `{模块: [(探测名, callable)]}` 目标表，由门面注入 vendors 函数——探测层永不 import 供应商代码；
- **横切关注点收拢**：按源 sleep、头伪装、腾讯裸 GET 统一进 `VendorHttp`，vendors 函数第一参数即 `http`；
- **可测试性从 0 到 1**：解析器/格式转换从 1905 行类里的私有小段，变成可用固定 fixture 直接测的纯函数（腾讯 kline JSON、行情行、THS 表格各有黄金用例）；
- 兼容重导出：`DataSourceConfig`/`ProbeResult` 仍可从 `data_access` 导入（零外部引用，双保险）。

## 施工方法（供未来拆大文件参考）

1. 先 grep 锁定外部调用面（15 个方法 + 构造签名），确定"什么绝对不能动"；
2. 通读全文按 self 依赖分簇（纯函数簇 → HTTP 原语簇 → 供应商簇 → 探测簇 → 编排簇）；
3. **从最纯的层往外拆**：纯函数先走（立即可测），最后才收敛门面；
4. 每步跑全量测试 + 编译；旧私有方法名全库 grep 确认零残留。

## 本轮文件清单

新增：`ticker_formats.py`、`response_parsers.py`、`vendor_http.py`、`capability.py`、`vendors/`（5 文件）、`tests/test_data_access_split.py`（43 用例）；重写：`data_access.py`。

## 建议提交

```bash
git add tradingagents/screener tests/
git commit -m "refactor(screener): split 1905-line ScreenerDataAccess god class" -m "- data_access.py 1905 -> 546 lines, now a pure facade
  (public API: 19 methods, names/signatures/fallback order preserved)
- vendors/{tencent,sina,ths,misc,backup}: per-vendor fetch functions,
  one change reason each
- capability.py: probing + capability matrix + probe cache (probe targets
  injected as callables — never imports vendor code)
- vendor_http.py: politeness sleep/spoof/raw-HTTP collected in one place
- ticker_formats.py + response_parsers.py: pure functions, now unit-testable
- tests: +43 (parser fixtures, ticker formats, capability builders,
  facade surface freeze, MarketDataPort protocol conformance) — 270 total"

git add "*.md"
git commit -m "docs: add phase-4 data-layer split construction log"
```

## 重构路线图终局

至此 `REFACTORING_ROADMAP.md` 与报告 3 列出的**全部主体工程完成**：
Phase 0 入口收口 ✅ / Phase 1 新旧 CLI 收口 ✅ / Phase 2 图驱动统一 ✅ / Phase 3 状态 canonical 化 ✅ / Phase 4 数据端口+依赖环+类型化错误+ScreenerDataAccess 拆分 ✅。

剩余为可选增量：v2.1 停写平铺状态字段（等二开稳定后）、逐类收窄 171 处 `except Exception`（随 bug 修复进行）、`Reflector`/`StructuredMemory` 千行类拆分（等有真实改动需求再做）。**项目正式进入二开阶段。**

---

# 加更四：契约层建设——AnalysisRequest/Result + AnalysisService + 执行事件协议（2026-08-16 第五轮施工）

> 验证：**295 个测试全绿**（270 + 25 新增，27 秒）；gitnexus：3 文件 / 16 符号 / 2 条流程，风险 medium（全部落在 Analyzer 执行链，预期范围）
> 本轮补完路线图 Phase 1 的核心三件套——原五大系统性问题的最后一块架构残留（"UI 消费引擎内部状态"）就此清零。

## 新增：`tradingagents/application/` 应用层包

| 文件 | 内容 |
|---|---|
| `contracts.py` | **`AnalysisRequest`**（frozen dataclass：ticker/trade_date/分析师/模型/思考配置；`from_questionnaire()` 兼容旧问卷 dict，`default_for()` 供非交互，`to_graph_config()` 产出图配置）+ **`AnalysisResult`**（决策/置信度(None 不造假)/耗时/用量/工件路径/final_state；`to_dict()` 保持 ui/summary.py 的 10 个键不漂移，有测试冻结） |
| `events.py` | **9 种执行事件**（`AnalysisStarted/Completed`、`MessageEmitted`、`ToolCallObserved`、`ReportSectionUpdated`、`AgentStatusChanged`、`TimelineNoted`、`StageMarked`、`MetricsUpdated`）+ **`ChunkEventTranslator`**——全仓唯一允许理解 LangGraph chunk 内部的地方（消息分类/去重、分析师状态推导、辩论/交易员/风险级联全部原样迁入） |
| `service.py` | **`AnalysisService`**：`stream_events(request)` 返回事件流（图**急切构造**——坏配置在任何 Live 上下文之前失败）、`run(request, on_event)` 无头执行；`AnalysisEventStream.result` 在耗尽后持有类型化结果 |

## 改造

- **`cli/analyze/run_impl.py` 从 458 行重写为纯 UI 适配**：`_apply_event()` 把事件映射到 MessageBuffer/仪表盘，不再出现任何 chunk 字段（有 AST 测试钉死 `analyst_reports`/`debate_blocks`/`decision_blocks`/`messages` 四个字符串永不回流此文件）；
- **`__main__.py` 非交互分支**改用 `AnalysisRequest.default_for()`，删除手拼 dict；
- `app.py` 问卷路径零改动（`from_questionnaire` 兼容）。

## 架构意义

```text
之前：CLI 问卷 dict → run_impl（执行+状态解读+UI）→ 图
现在：CLI / Python API / 未来 Web API
         └→ AnalysisRequest（类型化）
              └→ AnalysisService（应用层）
                   ├→ 事件协议（稳定契约，测试冻结）
                   └→ 图 runtime（内部状态不再外泄）
```

新增消费方（Web API、批处理、静默模式）只需消费 `run(request, on_event)`——不再需要复制任何"如何解释状态"的知识。

## 新测试（+25）

- `test_execution_events.py`（17）：消息分类/截断/去重、工具调用提取、结构化优先报告检测、平铺兜底、分析师状态推导、全员完成→Bull 启动、辩论级联（judge→Trader→Stage C）、风险全员完成、指标轮询；
- `test_analysis_service.py`（5）：契约往返（问卷 dict→request→graph config）、默认请求、result dict 形状冻结、图急切构造失败语义、无头 run 事件收集；
- `test_state_canonical.py`（+1/-3 旧迁移测试）：run_impl 纯净度 AST 断言。

## 建议提交

```bash
git add tradingagents/application cli/analyze/run_impl.py tradingagents/__main__.py tests/
git commit -m "feat(application): typed contracts, AnalysisService, execution event protocol" -m "- AnalysisRequest/AnalysisResult typed contracts (dict compat kept,
  result-key shape frozen by tests)
- AnalysisService.stream_events()/run(): graph constructed eagerly,
  headless consumption supported via on_event callback
- ChunkEventTranslator: the single place that understands LangGraph chunks
  (message dedup, analyst statuses, debate cascade ported verbatim)
- cli/analyze/run_impl.py: pure event->UI adapter, zero chunk field reads
  (enforced by AST test); --no-interactive path uses AnalysisRequest
- tests: +25 (events, contracts, service w/ stub graph) — 295 total"

git add "*.md"
git commit -m "docs: add contract-layer construction log"
```

## 剩余清单更新

原"A 组：契约层建设"三项全部完成 ✅。剩余：CI 流水线、README 清理（两小件）、`cli` 顶层包名迁移（破坏性，建议等下一次大版本）、以及按需牵引项（Merger 拆分、setup_graph 子图化、Reflector/Memory 拆分、供应商 contract tests、Phase 5 模型层、v2.1 状态里程碑）。
