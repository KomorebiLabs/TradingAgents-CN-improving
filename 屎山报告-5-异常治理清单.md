# 屎山报告 5：`except Exception` 治理分类清单

> 日期：2026-08（对应交接文档任务 G）
> 范围：全仓生产代码 `except Exception` 逐处核验（grep 实证，2026-08 快照）
> 现状数字：**53 处** `except Exception`（21 个文件），裸 `except:` = 0。
> 说明：报告 1 诊断的 171 处是删除旧层（commands/、screener/cli/）之前的基线；旧层删除 + 历轮拆分后实际基数 53。
> 治理原则（交接文档）：**跟着 bug 修，不做专项大扫除**。本清单按优先级标注，供随修随用。

---

## 分类总览

| 类别 | 数量 | 含义 |
|---|---|---|
| **A 高优先级·静默吞错** | 14 | `except Exception: pass` 或静默回退，掩盖可能的编程错误 |
| **B 中优先级·可收窄** | 20 | 预期失败（供应商/数据缺失），降级合理但应改用类型化错误/加日志 |
| **C 低风险·合理保留** | 19 | 可选依赖守卫、解析容错、探测契约、已带日志的降级 |

---

## A 类：静默吞错（14 处）— 该删捕获或至少加日志

> 共性：`except Exception: pass`，异常无日志、无类型化，编程错误（KeyError/TypeError/AttributeError）会被一起吞掉。

| # | 位置 | except 后行为 | 建议 |
|---|---|---|---|
| A1 | `trading_graph.py:293` | `return []`（`_safe_tools` 工具创建失败） | 报告 2 点名：分析师在无工具状态下跑完你不知情。改 `logger.warning/exception` + 保留 `[]` |
| A2 | `interface.py:121` | `pass` 后 `return str(result)` | ⚠️ 若 `result` 未定义会二次 NameError；确认后删 `except` 或给 `result` 兜底 |
| A3 | `deep_analyzer.py:322` | `pass`（提取 capital_heat_quality_gap_score） | 收窄为 `KeyError/TypeError` 或删，让逻辑错误暴露 |
| A4 | `deep_analyzer.py:328` | `pass`（提取 conflict_tier） | 同 A3 |
| A5 | `deep_analyzer.py:407` | `pass`（selected analysts 推断） | 加 `logger.warning` 保留兜底 |
| A6 | `universe.py:137` | `pass`（指数成分获取分支） | 加日志，确认是哪类失败 |
| A7 | `universe.py:429` | `return []` | 收窄为 `DataNotFound`，避免吞掉数据格式 bug |
| A8 | `universe.py:493` | `pass` | 加日志 |
| A9 | `universe.py:503` | `pass` | 加日志 |
| A10 | `live_dashboard.py:281` | `pass`（chunk 处理） | UI 层可保留兜底，但至少 `logger.exception` |
| A11 | `live_dashboard.py:287` | `pass` | 同 A10 |
| A12 | `agents/__init__.py:43` | `pass`（包级 import 时） | 若为可选依赖守卫 → 改归 C；若吞真错误 → 加日志区分 |
| A13 | `graph/__init__.py:26` | `pass`（包级 import 时） | 同 A12 |
| A14 | `screener/config.py:12` | `pass`（配置加载回退） | 加 `logger.warning`（配置缺失的原因要可诊断） |

---

## B 类：可收窄为类型化错误（20 处）— 预期失败，降级合法，但当前吞太宽

> 类型化错误地基已铺好：`dataflows/errors.py`（`VendorError` → `VendorUnavailable` / `VendorRateLimited` / `DataNotFound` / `VendorSchemaChanged`，均继承 `RuntimeError`）。

| # | 位置 | 现状 | 建议收窄为 |
|---|---|---|---|
| B1-7 | `y_finance.py:360,515,628,704,764,825,882` | 数据取回失败 → 返回错误字符串给 LLM | `except (VendorUnavailable, DataNotFound)`；保留返回错误串语义 |
| B8 | `yfinance_news.py:290` | 新闻获取失败 → 返回错误串 | 同 B1-7 |
| B9 | `yfinance_news.py:455` | 全球新闻失败 → 返回错误串 | 同 B1-7 |
| B10 | `alpha_vantage_indicator.py:641` | 指标失败 → print + 返回错误串 | `except VendorUnavailable`；print 改 logger |
| B11 | `alpha_vantage_common.py:458` | 过滤失败 → 返回原始数据 | `except DataNotFound`；print 改 logger |
| B12 | `deep_analyzer.py:140` | 分析异常 → `_dry_run` 回退 | 保留回退 + `logger.exception` |
| B13 | `data_access.py:414` | `pass`（供应商数据分支） | 收窄为 `VendorUnavailable` |
| B14 | `data_access.py:426` | `pass` | 同 B13 |
| B15 | `data_access.py:438` | `return None`（解析） | `except DataNotFound` |
| B16 | `data_access.py:440` | `return None` | 同 B15 |
| B17 | `vendor_http.py:84` | HTTP 失败 → `return None` | `except VendorUnavailable` |
| B18 | `cn_indicators.py:107` | **已有 logger.error** ✅ | 仅加 `except (VendorUnavailable, DataNotFound)`，日志保留 |
| B19 | `cn_indicators.py:154` | **已有 logger.error** ✅ | 同 B18 |
| B20 | `interface.py:361` | **已有 `_is_rate_limit_error` 识别** ✅ | 补捕获 `VendorRateLimited`；其余收窄为 `VendorUnavailable` |

---

## C 类：合理保留（19 处）— 不动

> 可选依赖守卫 / 解析器容错契约 / 探测语义（失败=不可用）/ 已带日志与错误收集的降级，均属正确用法。

| 位置 | 为什么保留 |
|---|---|
| `deep_analyzer.py:18`、`name_resolver.py:17` | `# pragma: no cover` 可选依赖 import 守卫 |
| `response_parsers.py:149,204,239` | 解析器对畸形输入返回 `None` = 公开契约，正常 |
| `response_parsers.py:250` | `pass` 后 `return normalized`（有兜底值，仅在复用上游变量时冒险——见 A 类备注） |
| `capability.py:63,73,117,451` | 能力探测：失败 = 该模块不可用，是探测语义本身 |
| `name_resolver.py:127,173,195` | 已有 `_warnings.append` / `errors.append` 错误收集 ✅ 好实践 |
| `throttling.py:47` | 已有 `_failed_requests += 1` / `_last_error_detail` 记录 ✅ |
| `screener/engine.py:168` | `drop_reason = "no_hist_data"` —— 有语义降级（进漏斗统计） |
| `trading_graph.py:85` | 图执行异常 → `_create_fallback_state`（核心容错，保留 + 已有调用方语义） |
| `trading_graph.py:108` | 决策提取失败 → `decision = "N/A"`（UI 有 N/A 展示语义） |
| `trading_graph.py:564` | "Memory persistence must never crash" 显式注释 + `pass` —— 保留但建议补 `logger.exception`（报告 2 原话"连 log 都没有"） |
| `terminal_mascot.py:24` | UI 装饰回退，无在意外影响 |

---

## 执行建议（供随 bug 修复时取用）

1. **先动 A 类**（14 处）：每处要么删捕获让其冒泡、要么补 `logger.exception/warning`。最高危：A1（`_safe_tools`）、A2（`interface.py:121` 潜在 NameError）。
2. **B 类跟随供应商/数据 bug 修**：把 `except Exception` → `except (VendorUnavailable, DataNotFound, VendorRateLimited)`，语义不变但失败有了名字。
3. **C 类不要动**——它们已经是对的。
4. 每改必跑 `pytest -q`（当前 347 全绿基线）。

> 待治理目标：A 类清零、B 类全部类型化、C 类保持。完成后 `grep -rn "except Exception" --include=*.py tradingagents | grep -v test` 应只剩 C 类残余。
