# 面试备考：预设陷阱与背诵答案

> 用法：简历里故意埋进下面的技术点 → 面试官会顺着问 → 你背下面的答案。每个"陷阱"都带：触发词（简历里怎么写）、预测追问（3 层）、背诵答案（真实可守住）。
> 原则：答案全部来自本项目真实实现，背熟即可扛住追问；括号内是记忆锚点，背不下来就抓锚点现场组织。

---

## 陷阱 1 · 回测 82.86% + 未来函数审计 ⭐最危险也最值钱

**简历触发词**：`自研信号回测引擎（复用真实选股逻辑），显式未来函数审计` + `总收益 82.86% / 夏普 2.17`

**预测追问**：
- Q1：82.86% 是怎么算的？窗口多长、池子多大？
- Q2：怎么证明没有未来函数泄漏？
- Q3：含交易成本吗？如果含了会差多少？

**背诵答案（Q1）**：用 2025-07 到 2026-06 共 242 个交易日，池子是沪深 300 成分股确定性切片 80 只，每 20 个交易日一次月度再平衡，按系统真实的技术面评分选 top5 等权持有，资金曲线逐日按持仓计算。（锚点：242 天 / 80 池 / 20 天 / top5）

**背诵答案（Q2）**：关键在设计——每个信号日评分时，取数一律截止到当天：`_load_histories` 里 `end_date = trade_date`，只用信号日之前的数据计算动量/均线/波动率。所以每个信号都是"当时拿得到的信息"，不是事后诸葛亮。我还专门核过这一条，写进了代码注释和测试。（锚点：end_date = trade_date）

**背诵答案（Q3）**：诚实回答——**当前未计交易成本**，所以 82.86% 是"毛收益"。我也不回避这一点，主动跟面试官说：这个项目的价值不在"数字好看"，而在**评估方法论完整**（可复现、可加成本、可切窗口），数字本身我也会按市况切片展示。主动说破比被问到再承认可信度高一大截。

---

## 陷阱 2 · Canonical State（schema v2）——"状态双写"迁移教科书

**简历触发词**：`Canonical State：结构化块权威、平铺字段降级兼容镜像`

**预测追问**：
- Q1：为什么同一份数据要存两套形状？
- Q2：谁写结构化、谁写平铺？方向会不会乱？
- Q3：你怎么定义"迁移完成"的边界？

**背诵答案**：这是从旧代码继承的迁移期问题——老节点只写平铺字段（market_report、final_trade_decision），新架构把它收敛到结构化块（analyst_reports、decision_blocks）。直接删平铺会砸掉还依赖它的老消费者（路由、日志、UI）。所以方案是：**声明结构化是唯一权威（canonical），平铺降级为只读兼容镜像**，用一个 `_ensure_structured_state` 在每次执行结束后双向补缺失、冲突时结构化胜，并引入 `schema_version` 做演进标记。加一个新字段现在只需要动一处，平铺镜像自动跟上——迁移成本从"×3"降回"×1"。（锚点：canonical / 镜像 / 补缺失 / 结构化胜 / schema_version）

---

## 陷阱 3 · 事件协议 + 契约层——"UI 不读 LangGraph 内部"

**简历触发词**：`9 种执行事件 · 契约层（AnalysisRequest/Result）· UI 与引擎解耦`

**预测追问**：
- Q1：为什么不让 UI 直接读 state？
- Q2：这 9 种事件具体是哪些？
- Q3：换成 Web API 或批量服务要改多少？

**背诵答案（Q1+Q3）**：早期 UI 直接消费引擎内部状态，一旦改内部结构 UI 就炸。我加了一层应用层：类型化请求 `AnalysisRequest`、结果 `AnalysisResult`、和一个**全仓唯一能懂 LangGraph chunk 的翻译器**，把它转成稳定事件流（消息、工具调用、报告更新、分析师状态、辩论级联、指标……）。任何消费方只要 `run(request, on_event)` 就能接入——CLI 是第一个消费方，未来 Web API 是第二个，代码零复制。（锚点：唯一翻译器 / 稳定协议 / run+回调）

---

## 陷阱 4 · Behavior-Preserving Refactor——"重构不是重写"

**简历触发词**：`golden + parity 测试证明输出逐字节等价，公开 API 零改动`

**预测追问**：
- Q1：你拆 1000 行的文件，怎么保证没改坏行为？
- Q2："逐字节等价"具体怎么验证的？
- Q3：为什么不直接重写更干净？

**背诵答案（Q1+Q2）**：两步。第一步先写 **golden 测试**把现状行为冻结下来（真实输入→真实输出全记成断言）；第二步才动结构，每次拆完跑同一条测试。更强的还有 **parity 测试**：把原文件 `git mv` 成 legacy、新包并行，同一组输入分别喂两边，`model_dump()` 逐字节比对，用 8-18 条用例钉死"新包输出 == 旧实现输出"。所以"等价"不是靠感觉，是机器比的。（锚点：先冻结再拆 / legacy 双跑 / 逐字节）

**背诵答案（Q3）**：重写看着干净，但 1905 行里藏着调研公司实际业务逻辑、边界行为，重写 = 无声删掉细节 + 引入新 bug。行为保持重构 + 逐字节验证，是在"给后来者一个可信的迁移路径"，比推到重来工程上更负责任。

---

## 陷阱 5 · 依赖单向无环 + AST 测试钉死

**简历触发词**：`One-Way Layering：CLI→Application→Graph→Ports→Dataflows，AST 无环测试`

**预测追问**：
- Q1：你怎么保证依赖不反向？
- Q2："AST 无环测试"是怎么写的？

**背诵答案**：用 Python 的 `ast` 解析所有模块的顶层 import，建依赖图，跑 **Kahn 拓扑排序**——有环就报错。我把它写成一个离线测试，任何新代码只要引入环，CI 直接红灯。曾抓过一个具体案例：`dataflows` 反向 import `screener`，每次调用还 new 一个新实例导致限流器失效——这类问题从此被机器挡住，不靠人自觉。（锚点：ast / Kahn / CI 红灯）

---

## 陷阱 6 · 六大千行文件拆解 + 门面重导出

**简历触发词**：`merger 1050 / reflection 1302 / memory 1124 / data_access 1905 … 全部分解`

**预测追问**：
- Q1：拆这么多文件，消费方代码改了多少？
- Q2：为什么有的拆成包、有的留门面？

**背诵答案**：公开 API 零改动——每个大文件拆完留一个**门面**（薄转发重导出，几行），所有 `from xxx import` 的 18 个消费方一行都没动。拆法按"一个文件只为一个变化原因而变"：1905 行的 data_access 拆成门面 546 行 + 五类供应商适配 + 纯函数解析器 + 限速器，每块单一职责。什么时候用门面？消费方多（agent_utils 18 个）就保门面；消费方少（merger 只有 engine 一个）直接改 import。（锚点：门面薄转发 / 单一变化原因 / 按消费方数决定）

---

## 陷阱 7 · 供应商健康监控 + 反爬重试（429/403 绝不重试）

**简历触发词**：`逐供应商健康监控 · 连接类重试 · HTTP 429/403 绝不重试`

**预测追问**：
- Q1：为什么 429 反而不重试？重试不是更稳吗？
- Q2：健康监控监控什么？

**背诵答案（Q1）**：两个失败是**不同类型**：连接重置/超时是瞬时网络抖动，重试合理（指数退避 1s→2s）；而 **429/403/5xx 是服务端的限流/封禁信号**——这时候重试等于向反爬系统确认"我是爬虫"，越重试封得越狠，甚至可能连累整个 IP。所以策略是：**只有连接类错误重试，HTTP 状态类一个都不重试**，直接降级到下一个数据源。（锚点：瞬时 vs 信号 / 重试=自曝爬虫 / 直接降级）

**背诵答案（Q2）**：一个线程安全计数器，挂了 27 个供应商函数的钩子，记录每次调用的**成功/失败/耗时/最近错误**，最后输出每个源的健康表（失败率、均值耗时）进筛选报告的 metrics。数据坏了不静默返回空——先有日志，后有健康表。

---

## 陷阱 8 · LangGraph 多智能体流程（基础必考）

**简历触发词**：`LangGraph 状态机：分析师→多空辩论→交易员→风控辩论`

**预测追问**：
- Q1：为什么用 LangGraph 而不是手写循环？
- Q2：辩论什么时候停？路由怎么决定？
- Q3：上下文太长怎么办？

**背诵答案（Q1）**：因为有向状态图能力恰好匹配：**状态聚合**（`Annotated[+operator.add]` 追加辩论历史）、**条件路由**（谁发言、是否够轮次）、**流式执行**（`graph.stream()` 给 dashboard 实时推送）。手写循环会把这些变成散落的 if/else。（锚点：聚合 / 条件路由 / stream）

**背诵答案（Q2）**：辩论轮数自适应：先看语义信号（政策地位、资金质量、冲突等级）决定加/减轮，再按 `count >= 2×rounds` 退出——简单标的少辩论省 token，高冲突标的加辩论防误判。（锚点：语义信号 / 轮数上限 / 省 token）

**背诵答案（Q3）**：阶段边界有 LLM 压缩节点，超过 token 阈值把前文压成结构化摘要再传下游；这也关联到那个**事件协议**——downstream 只认摘要+事件，不背原始长文。（锚点：阶段压缩 / 阈值 / 摘要再传）

---

## 陷阱 9 · BM25 本地记忆——"技术选型的克制"（看似简单实则加分）

**简历触发词**：`结构化记忆 + BM25 本地检索，刻意不引入向量库`

**预测追问**：为什么用 BM25 不用向量数据库？

**背诵答案**：场景是交易情境的**短文本精确匹配**（"科技板块高波动"这种 query），BM25 的词法匹配够用且不需要 GPU/embedding 服务、完全离线可跑、任何 LLM 都兼容。我评估过向量库：加模型下载、加服务依赖，收益在这个场景不明显。**不用一个技术不是因为不会，而是判断它不值**——这反而是被问时可以展示"技术选型判断力"的地方。（锚点：场景匹配 / 离线 / 不引入 = 判断力）

---

## 备考提示（通用）

1. **每个陷阱背"锚点词"不背整句**——面试现场用锚点现场组句，比背稿自然得多；
2. **主动暴露 2-3 个"短板"**（如回测未计成本）——面试官往往抓住你主动说破的点放你一马，而怀疑你没说的点；
3. **口径一句话**：面试官问"82.86% 怎么测的"→ 你就答"242 天回测，池 80 只，月度再平衡，基准沪深300"——一句话给全，不展开等追问；
4. **热词能定义**：说出"Canonical State""Event Protocol"前，先在心里过一遍它是什么、在哪用、为什么——卡壳就换平实说法（这是防伪审计的核心）。
