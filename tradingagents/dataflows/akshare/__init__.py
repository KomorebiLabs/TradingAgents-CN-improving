"""AkShare vendor implementations, split by domain.

Modules: stock / news / flow / macro / events / financials, over _shared
infrastructure. The public API is re-exported by dataflows.akshare_interface
(the facade the VENDOR_METHODS registry resolves against).
"""

