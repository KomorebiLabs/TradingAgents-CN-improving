# 屎山报告 1/3：现状总览（2026-08-16 删除后基线）

> 审阅人：ZCode（第二轮深度审阅）
> 前置材料：`PROJECT_ARCHITECTURE_REVIEW.md`（2026-08-09，删除前）、`REFACTORING_ROADMAP.md`、`清洗.md`
> 本轮方式：不派 subagent 狂读，直接逐步抽读地基文件 + 交叉验证 grep，每条结论都有当前代码的 file:line 证据。
> 本轮**未修改任何业务代码**。

---

## 一句话结论

文件层面的屎（调试脚本、过时文档、外部参考仓库）你已经删干净了；但**代码内部的屎一处都没动**——171 处宽泛异常捕获与删除前完全一致，七大根基问题（见报告 2）全部健在，并且比上一轮体检时**恶化了两处**：四对"完全一致"的重复文件已经分叉漂移，安装入口被发现直接指向旧引擎。

---

## 1. 删除战果确认

对照 `清洗.md` 的分级，当前状态：

| 清洗.md 分级 | 状态 |
|---|---|
| A 类（build/、egg-info、缓存、commit_msg.txt 等） | ✅ 已删除 |
| docs/（Plan、Learning、debug_artifacts 共 80+ 文件） | ✅ 已删除（git 显示 D） |
| tests/（43 个文件） | ✅ 已删除 |
| reference/openharness（外部框架副本，90+ 文件） | ✅ 已删除 |
| Prompt_notSkill/ | ✅ 已删除 |
| B 类 `-no` 目录（未跟踪隔离区） | ⏳ 仍在磁盘上，待你决定 |
| C 类重复实现（旧 Analyzer/Screener CLI） | ❌ 未收口，且已分叉（见 §4） |

删除没有误伤：`cli/`、`tradingagents/` 主链文件全部健在。

## 2. 当前家底（数字）

| 指标 | 数值 |
|---|---|
| Python 文件（tradingagents + cli + main.py） | 150 |
| 物理总行数 | 39,263 |
| 版本号 | **两套**：`pyproject.toml:7` = 0.2.3，`tradingagents/__init__.py:12` 和 `__main__.py:21` = 2.0.0 |
| 活跃测试 | **0**（tests/ 已删，`pyproject.toml:44-47` 的 pytest marker 成为空壳） |
| 宽泛 `except Exception` | 171 处（与删除前完全一致） |
| 裸 `except:` | 0 处（这点及格） |
| 已确认死代码文件 | 2 个 + 1 段死函数（见报告 3 清单） |
| 已确认模块依赖环 | 1 个（4 模块，靠延迟导入苟活） |

## 3. 巨型热点（当前实测行数）

| 文件 | 行数 | 职责混杂度 |
|---|---:|---|
| `screener/data_access.py` | 1,905 | 一个类 = 供应商注册表 + HTTP 客户端 + 解析器 + 探测器 + 能力矩阵 + 限流器 + 反爬伪装 |
| `dataflows/akshare_interface.py` | 1,619 | 全部"供应商"实际实现都住在这一个文件里（见报告 2 §5） |
| `graph/reflection.py` | 1,302 | Reflector 一个类做反思、路由统计、记忆写入、结论摘要 |
| `commands/analyze/app.py` | 1,213 | 旧 Analyzer：UI+执行+状态+存储一体 |
| `agents/utils/memory.py` | 1,124 | StructuredMemory：存储+BM25+过滤+统计+趋势 |
| `screener/merger.py` | 1,050 | 聚合+冲突+硬过滤+语义解释+行业分散+排序 |
| `agents/utils/agent_utils.py` | 944 | 工具装配+工具函数大杂烩 |
| `graph/setup.py` | 829 | 其中 `setup_graph()` 单方法 471 行（357-828） |

另一个极端：`dataflows/config.py` 全文 368 行，**真实代码约 40 行**，其余全是 ASCII 艺术图式的"教学作文"注释（见报告 2 §7）。

## 4. 本轮新发现（上一轮报告没有的）

### 4.1 重复文件从"复制"恶化成"分叉"

上一轮（08-09）记录为"完全一致"的四对文件，本轮 diff 实测：

| 文件对 | 当前差异 |
|---|---|
| `cli/config.py` ↔ `commands/analyze/config.py` | 12 行不同 |
| `cli/models.py` ↔ `commands/analyze/models.py` | 20 行不同 |
| `cli/stats_handler.py` ↔ `commands/analyze/stats_handler.py` | **152 行不同** |
| `cli/utils.py` ↔ `commands/analyze/utils.py` | **720 行不同** |

含义：你以为在维护一份代码，实际在维护两份正在漂移的代码。每改一处 bug，另一处悄悄留着旧 bug。这是复制型屎山的标准恶化轨迹。

### 4.2 安装入口指向旧引擎（打包炸弹）

`pyproject.toml:36`：

```toml
[project.scripts]
tradingagents = "cli.main:app"
```

而 `cli/main.py` 的实际行为是：

1. import 时检查 questionary，缺失则 `sys.exit(1)`（`cli/main.py:34`）；
2. 存在则转发到**旧的** `tradingagents.commands.analyze.app`（`cli/main.py:37`）。

也就是说：`pip install` 之后敲 `tradingagents` 命令 = 直接进旧 Analyzer，**完全绕过**你在 `tradingagents/__main__.py` 里写的新统一 CLI（analyze/screener/report 子命令 + 主菜单）。新 CLI 只有 `python -m tradingagents` 这一条命。

附带伤害：`pyproject.toml:39` 把 `cli*` 作为顶层包打进安装包——你的 site-packages 里会多出一个叫 `cli` 的通用顶层包名，和任何其他工具的 `cli` 模块直接撞名。

### 4.3 新 CLI 反向依赖旧层，旧层删不得

新 `cli/` 有 5 处 import 旧 `tradingagents.commands.analyze` / `tradingagents.screener.cli`：

- `cli/main.py:37`（转发到旧 app）
- `cli/main_menu.py:104, 119, 129`（三个 fallback）
- `cli/__init__.py:15`（from old commands import app, run_analysis）
- `cli/analyze/app.py:65`（从旧层导入 fetch_announcements）

其中最典型的是最后一条：`cli/analyze/app.py` 注释里写着"announcements 在旧 commands 层"，于是新代码回头去旧层拿函数。**迁移成了环**：旧层没人引用新层，新层却断不了旧层的奶。

### 4.4 新 CLI 的执行引擎绕过公开 API、直捅图内部

`cli/analyze/run_impl.py` 不调用 `TradingAgentsGraph.propagate()`，而是自己重写了整个流循环：

- `graph.propagator.create_initial_state(...)`、`graph.propagator.get_graph_args(...)`（run_impl.py:210-213）
- `graph.graph.stream(...)`（run_impl.py:218）
- 跑完后调用**私有方法** `graph._synchronize_structured_state(trace[-1])`（run_impl.py:261）

同文件还残留 6 处 `print("[DEBUG] Step N...")` 生产调试打印（如 run_impl.py:209、217），以及 `except Exception: pass` 吞掉决策提取失败（run_impl.py:263-265）。

结果：现在有**三个平行的图执行驱动器**（`propagate()`、新 run_impl 的流循环、旧 app 的 run_analysis），行为各自为政，任何一个改动都要人肉同步另外两个。

## 5. 当前系统真实的启动路径图

```text
pip 安装的 tradingagents 命令 ──► cli/main.py ──► 旧 commands/analyze/app（旧引擎）
                                                        ▲
python -m tradingagents ──► tradingagents/__main__.py ──┤ try/except ImportError
python -m cli ──► cli/main_menu.py ────────────────────┤ 三处 fallback 都落回旧层
python main.py ──► TradingAgentsGraph.propagate()（硬编码 000001 / 2026-04-28 的个人脚本）
```

三条入口、两个引擎、若干条 fallback 暗道。这就是"基本功能能跑"背后的真实拓扑。

## 6. 健康度评分（当前）

| 维度 | 评分 | 变化 vs 上轮 | 说明 |
|---|---:|---|---|
| 仓库卫生 | 4/5 | ↑ | 文件级垃圾基本清完，剩 `-no` 目录待决 |
| 模块边界 | 2/5 | → | 依赖环、反向依赖、双入口全部健在 |
| 可测试性 | 1/5 | → | 依然 0 测试，且旧 tests 删了后连参考都没了 |
| 可发布性 | 1/5 | ↓ | 安装入口指向旧引擎 + 顶层 `cli` 包污染是本轮新实锤 |
| 一致性 | 2/5 | ↓ | 重复文件已分叉，版本号仍两套 |

**综合：2/5。** 病根一个没动，且"复制型屎山"正在向"分叉型屎山"演化——后者修复成本随时间指数上升。

---

下一步看报告 2（根基屎在哪、为什么是根基）、报告 3（哪些还能删、先改什么才能安全二开）。
