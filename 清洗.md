# TradingAgents 清洗清单

> 说明：本文件只列「建议删除/闲置」的文件夹与文件，**不由我删除，由你确认后自行操作**。
> 分级：**A 无条件可删**（垃圾/产物/缓存） → **B 已隔离的 `-no` 目录**（你确认后删除或归档） → **C 重复实现**（先收口再删，不要直接删） → **D 不要删**（核心资产）。
> 已核对：每条依据来自当前仓库实际内容/引用，非猜测。

---

## A. 无条件可删（构建产物 / 缓存 / 临时文件）

| 路径 | 内容/原因 |
|---|---|
| `build/` | pip 构建产物（59 个文件），`python -m build` 可随时重新生成 |
| `tradingagents.egg-info/` | 安装元数据，`pip install .` 自动重建 |
| `.tradingagents/` | 运行时缓存（cache/screener/ 等，3 个文件），程序自动重建 |
| `.tmp_screener_test/` | 筛选器临时测试输出 JSON（5 个文件） |
| `.pytest_cache/` | pytest 缓存 |
| `__pycache__/`（各目录下） | Python 字节码缓存，自动生成 |
| `commit_msg.txt` | 已提交 commit 的信息草稿，与历史重复 |
| `commit_msg_cn_indicators.txt` | 同上 |
| `commit_msg_ui.txt` | 同上 |
| `requirements.txt` | 仅一行 `.`，依赖全在 `pyproject.toml`；无人用 `pip install -r` 则可删（可选） |

---

## B. 已隔离的 `-no` 目录（当前均为 git 未跟踪状态）

| 路径 | 内容/原因 |
|---|---|
| `Prompt_notSkill-no/` | 旧 Agent Prompt 草稿（非 Skill），已隔离 |
| `docs-no/` | 旧 Plan / Learning / debug_artifacts 调试脚本等，已隔离 |
| `reference-no/` | 外部框架副本（openharness 等），非本项目源码 |
| `rag_data-no/` | RAG 数据缓存 |
| `reports-no/` | 历史报告输出 |
| `tests-no/` | 旧测试（未读取，仅按隔离处理） |

> 注意：这些目录当前是 `??` 未跟踪。删除它们后，需在 git 里处理原路径的删除记录（`docs/`、`reference/`、`tests/` 等在暂存区仍显示 `D`），仓库才算真正干净。删除前请确认不再需要任何内容。

---

## C. 重复实现（先收口，后删除——不要直接删）

> 这些是同一迁移期的双实现。直接删除会破坏仍在引用它们的旧入口。正确顺序：先把入口统一到新路径 → 验证 → 再删旧目录。删除前请用 `git grep` 二次确认。

**内容完全重复（逐字符一致）：**
- `cli/config.py` ↔ `tradingagents/commands/analyze/config.py`
- `cli/models.py` ↔ `tradingagents/commands/analyze/models.py`
- `cli/stats_handler.py` ↔ `tradingagents/commands/analyze/stats_handler.py`

**高度重复：**
- `cli/utils.py` ↔ `tradingagents/commands/analyze/utils.py`（约 99%）
- `cli/screener/run_impl.py` ↔ `tradingagents/screener/cli/commands/run_impl.py`（约 62%）

**疑似死代码（删前 grep 确认）：**
- `cli/announcements.py` — 无任何入站引用，内容与旧 `commands/analyze/announcements.py` 一致
- `tradingagents/agents/risk_mgmt/aggressive_debator.py` — 与 `aggressive_debater.py` 内容几乎一致，且无有效入口引用（运行链用的是 `aggressive_debater.py`）

**退役候选（收口后整目录删除）：**
- `tradingagents/commands/analyze/` — 旧 Analyzer，新路径 `cli/analyze/` 已接手
- `tradingagents/screener/cli/` — 旧 Screener CLI，新路径 `cli/screener/` 已接手

---

## D. 不要删（核心资产）

- `tradingagents/` 下所有有引用的业务代码（如 `graph/`、`agents/`、`screener/`、`dataflows/`、`llm_clients/`、`harness/`、`ui/`）
- `cli/` 下的 `app.py`、`main_menu.py`、`prompts.py`
- `pyproject.toml`（版本与依赖唯一来源）
- `main.py`（Python API 入口）
- `README.md`、`README_TECH.md`（内容可整理，文件保留）
- `.env.example` 系列（配置模板）

---

## 建议操作顺序

1. **A 类**：直接删除，无风险，立即可做。
2. **B 类**：确认无需归档后删除，并在 git 里清理对应原路径的删除记录。
3. **C 类**：不要现在删。先按 `REFACTORING_ROADMAP.md` 的 Phase 1 收口入口，再删重复目录。
4. 删除后运行一次 `git status` 确认只剩预期变更，并跑一遍 `python -m tradingagents --version` 确认入口仍正常。
