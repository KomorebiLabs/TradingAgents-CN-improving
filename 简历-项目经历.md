# [姓名] · [电话] · [邮箱] · [所在城市]

**AI Agent / LLM 应用开发工程师** · 专注 Multi-Agent 编排、LangGraph 状态机、工具调用与 Agent 可观测性
github.com/KomorebiLabs/TradingAgents-CN-improving

---

## 项目经历

### TradingAgents-CN —— 基于 LangGraph 的多智能体 LLM 交易分析框架（A 股）

**一句话定位**：用 LangGraph 有向状态图编排「分析师团队 → 多空辩论 → 交易员 → 风控辩论 → 组合经理」的多智能体协作流程，把"几个 Prompt 串起来"的多 Agent 框架沉淀成**可编排、可观测、可评估、可二开**的 Agent 平台。

**我的职责**：核心作者，主导多智能体编排链路与 Agent 平台化能力——状态机、工具系统、记忆、评测；同时将历史遗留系统逐步收口为可演进架构。

#### Agent 编排（LangGraph）
- 用 `StateGraph` 建模多阶段流转：分析师阶段 → 研究辩论 → 交易 → 风控辩论 → 决策；
- **状态聚合**：`Annotated[..., operator.add]` 追加辩论历史（Bull↔Bear 史）、单值字段覆写——多 Agent 间信息如何累积与传递；
- **条件路由**：控制"下一个谁发言 / 轮次够不够退出"；辩论轮数**自适应**（按政策/资金/冲突语义信号增减，简单标的省 token）；
- `graph.stream()` 流式产出每个节点中间状态，驱动实时面板；
- **阶段压缩**：每阶段边界 LLM 压缩历史到结构化摘要（token 阈值约 18K），防上下文爆炸。

#### AgentState 状态设计（canonical schema v2）
- 结构化块（`analyst_reports` / `debate_blocks` / `decision_blocks`）为唯一权威，平铺字段降级为 legacy 兼容镜像；
- 双向补缺失、冲突时结构化胜 + `schema_version` 演进标记——**给 Agent 状态做 schema 治理**的成本从 ×3 → ×1。

#### 工具系统（Tool / Port 分层）
- 按标的（板块 / 风格 / 技能）**动态装配工具集**（`get_tools_for_analyst` + `instrument_profile`）；
- `Tool Router → MarketDataPort` 能力隔离：Agent 不绑死具体数据供应商，测试可注入 stub 全离线；
- **9 种执行事件**统一承载 Agent 状态 / 工具调用 / token / 成本 / 延迟，UI、无头 API、观测系统一个 `run(request, on_event)` 接入——多 Agent 的"可观测性"如何不侵入引擎。

#### 记忆与上下文（本地 BM25，刻意不引向量库）
- 6 个角色独立短期记忆（Bull / Bear / Trader / 裁判 / 组合经理）+ 结构化长期记忆（BM25 检索 + 反向索引 + 过滤算子）；
- 选型理由：交易情境是短文本精确匹配，BM25 离线、零 embedding 成本、兼容任意 LLM——**做了替 Agent 记忆做技术选型判断**。

#### LLM 工程
- 多提供商工厂（OpenAI / Google / Anthropic / DeepSeek / Qwen / GLM / xAI …10+）；
- **双模型分工**：`deep_think_llm`（复杂推理节点）+ `quick_think_llm`（快速分析节点）；
- 成本估算（`$/MTok`）+ **结构化输出提取**（决策正则优先、省一次调用）+ LRU 缓存（opt-in）。

#### Agent 可观测与可评估
- Token / 成本 / 工具调用全链路进 Harness；事件协议让"Agent 在干什么"可视化可审计；
- **消融框架**：分析师数 × 辩论深度的对照实验，量化"多智能体是否真的提升决策一致性"；
- **正确性评测集**：已知历史结局 → 前向收益标注 → 混淆矩阵 + 方向准确率（Agent 输出准不准，不再靠感觉）；
- 自研信号回测引擎（复用真实选股逻辑，显式防 look-ahead）——给 Agent 的"判断"一个可复现的评估基线。

#### 与上游开源版（TauricResearch/TradingAgents，约 99k★）的针对性重写 ⭐ 面试核心区

> 这不是把上游"换个壳"，而是发现它的核心编排层是"能跑但不值得学"的状态后，**带着判断重写了它的 Agent 骨架**。每条"上游 → V2"都是一段可以从实现讲到取舍的故事。

| 上游原版的问题 | 我重写成 | 关键取舍（面试可深挖） |
|---|---|---|
| Analyzer 是**线性流水线**：分析师→辩论→风控 顺序硬走，`setup.py` 仅 136 行 / 14 节点，辩论是 Bull/Bear 轮流发话、count 即停 | **LangGraph 状态机**：25+ 节点、4 个路由拦截器、条件路由 + 循环辩论、阶段压缩节点 | 状态该聚合在节点还是全局？循环怎么保证终止？ |
| `Reflector` 仅 49 行 / 1 个方法，反思输出 2-4 句"作文" | 反思拆成**抽取/路由分析/结论纯函数 + 反射服务**，按 Agent 逐类反思 | 反思到底要不要 LLM？纯函数和 LLM 的边界在哪？ |
| AgentState 是一堆**平铺字段**，谁写谁读没人说得清 | **Canonical State (schema v2)**：结构化块权威、平铺降级镜像、版本号演进 | 接触一个别人写的状态机，你怎么重构它的数据流？ |
| 单一 JSON `TradingMemoryLog`，没有跨会话智能 | **6 角色独立记忆 + 结构化记忆**（BM25 本地检索、反向索引、过滤算子） | 记忆该放哪层？检索用什么？为什么不引向量库？ |
| 工具**硬编码**在每个 Agent 里，加一个新标的要改 Agent 代码 | **按标的动态装配工具集** + `Tool Router → MarketDataPort` 能力隔离 + 事件可观测 | 怎么让 Agent 无感地用上新工具？ |
| 只有美股 yfinance 数据 | **A 股 5 级供应商降级链**（腾讯直连 → AkShare → 新浪 → BaoStock → yfinance）+ 政策/资金流/概念 | 数据供应商失败时怎么保证 Agent 不崩（熔断/降级/健康监控）？ |
| **无测试**，改一行不知道崩哪 | **439 个离线测试**（golden/parity 冻结行为，Agent 行为零漂移） | 多 Agent 系统的行为怎么冻结、怎么回归？ |
| 输出不可评估（靠人读报告） | **回测引擎 + 消融框架 + 正确性评测集**（决策一致性 / 混淆矩阵 / 方向准确率） | 你怎么证明"这个多 Agent 系统比单个 LLM 好"？ |

> 面试话术锚点：上游 99k★ 不等于质量——它的编排层是"演示级"；我做的是把这条 `Analyzer 线性链` 重写成可编排、可观测、可评估的 Agent 平台。上面每一条，面试官都可能从"上游长什么样？你改了什么？为什么这么改？"三连追问。（备考：`面试备考-预设陷阱与答案.md` + `README_TECH.md §1/§2/§3/§4`）

#### 工程严谨（平台可演进性的保证，一句话支撑）
- 439 个离线测试（golden / parity 冻结 Agent 行为，公开行为零漂移）；六大千行模块拆解为单向依赖分层；供应商 5 级降级 + 熔断 + 反爬重试。

**技术栈**：LangGraph · LangChain · Python · OpenAI / DeepSeek / Qwen 等多模型 · AkShare 多供应商 · Rich

---

## 陷阱映射（AI Agent 岗必问点 → 你的准备弹药）

| 简历触发词 | 面试官会追问 | 备考处 |
|---|---|---|
| `StateGraph` 状态聚合 `operator.add` | 为什么加法聚合？辩论历史怎么不重复叠加？ | 面试备考 · 陷阱 8 |
| 辩论轮数自适应 | 怎么决定加轮/减轮？阈值来源？ | 面试备考 · 陷阱 8 |
| 阶段压缩 token 阈值 18K | 超了会怎样？压缩丢信息怎么解决？ | 面试备考 · 陷阱 8 |
| canonical schema v2 | 为什么双写？方向谁权威？ | 面试备考 · 陷阱 2 |
| 9 种执行事件 / `run(request, on_event)` | 和直接读 state 的区别？Event 怎么分类？ | 面试备考 · 陷阱 3 |
| BM25 vs 向量库 | 为什么不用向量库？召回效果？ | 面试备考 · 陷阱 9 |
| 结构化输出提取 | 正则优先的失败边界？歧义怎么办？ | 面试备考 · 陷阱 8 |
| 消融 / 评测集 | baseline 是什么？准确率怎么算？ | 面试备考 · 陷阱 1 思路 |
| 回测防 look-ahead | 怎么保证工具只用当日数据？ | 面试备考 · 陷阱 1 |

> 岗位匹配说明：主体全部是 **AI Agent 工程能力**（编排/状态/工具/记忆/评测/LLM 工程），重构与治理仅作最后一句支撑——因为它是"平台可演进"的保证，不是岗位卖点。
